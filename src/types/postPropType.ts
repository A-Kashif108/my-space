
type PostProps = {
    user: string,
    like : number
    dislike:number,
    id:string,
    imageurl:string,
}

// these types needs to be instantiated so can be created as interface
export type {PostProps}